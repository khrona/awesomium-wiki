        <h1 class="header">
          <a href="{{ HOME_PATH }}">
            <img src="{{ ASSET_PATH }}/images/awe-trans-icon-logo.png" />
            <img class="delimiter" src="{{ ASSET_PATH }}/images/logo-delimiter.png" />Wiki</a>
          <div class="clear"></div>
        </h1>
        <p class="header"></p>
        
        <h3 class="header">Categories</h3>
        {% for g in site.groups %}
        <p class="header"><a href="{{ BASE_PATH }}{{g.path}}">{{ g.name }}</a></p>
        {% endfor %}
